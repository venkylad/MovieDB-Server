export const getWishlist = (req, res) => {
  res.status(200).json({ message: "get wishlists" });
};

export const postWishlist = (req, res) => {
  res.status(200).json({ message: "post wishlists" });
};

export const putWishlist = (req, res) => {
  res.status(200).json({ message: "put wishlists" });
};

export const deleteWishlist = (req, res) => {
  res.status(200).json({ message: "delete wishlists" });
};
